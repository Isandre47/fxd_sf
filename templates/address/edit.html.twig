{% extends 'base.html.twig' %}

{% block title %}Edit Address{% endblock %}

{% block body %}
<div id="container-main">
    <div class="centralizer w400px">
        <div class="main">
            <p class="breadcrumb"><a href="{{ path('home') }}">Accueil</a> > <a href="{{ path('home_account') }}">Votre compte</a> > <a href="{{ path('app_address_index') }}">Mes adresses</a> > Modifier une adresse</p>
            <h1>Modifier mon adresse</h1><br />
            {{ include('address/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <a href="{{ path('app_address_index') }}">Retour</a>

{#            {{ include('address/_delete_form.html.twig') }}#}
        </div>
    </div>
</div>
{% endblock %}
