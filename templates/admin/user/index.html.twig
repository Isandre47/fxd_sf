{% extends 'admin/base_admin.html.twig' %}

{% block body %}
    <h1>User index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Email</th>
            <th>IsVerified</th>
            <th>Name</th>
            <th>Surname</th>
            <th>actions</th>
            <th>Abonnements</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
                <td>{{ user.name }}</td>
                <td>{{ user.surname }}</td>
                <td>...</td>
                <td>
                    <a href="{{ path('test', {'id': user.id }) }}">show</a>
{#                    <a href="{{ path('app_user_edit', {'id': user.id}) }}"    >edit</a>#}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{#    <a href="{{ path('app_user_new') }}">Create new</a>#}
{% endblock %}
