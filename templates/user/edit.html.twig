{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div id="container-main">
    <div class="centralizer w400px">
        <div class="main">
            <p class="breadcrumb"><a href="{{ path('home') }}">Accueil</a> > <a href="{{ path('home_account') }}">Votre compte</a> > Vos informations</p>
            <h1>Mes informations personnelles</h1><br />

            {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <p>
                <a href="{{ path('home_account') }}">Retour</a>
            </p>
{#            {{ include('user/_delete_form.html.twig') }}#}
        </div>
    </div>
{% endblock %}
